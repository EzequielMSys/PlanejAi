const r=require('express').Router();const c=require('../controllers/turmaController');const{authMiddleware,isGestorPedagogico}=require('../middlewares/authMiddleware');r.use(authMiddleware);r.get('/',c.listar);r.get('/candidatos',isGestorPedagogico,c.candidatos);r.post('/',isGestorPedagogico,c.criar);r.get('/:id',c.detalhes);r.post('/:id/membros',isGestorPedagogico,c.adicionar);module.exports=r
