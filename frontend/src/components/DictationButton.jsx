import{useRef,useState}from'react';import{toast}from'react-hot-toast';
export default function DictationButton({onText}){const[active,setActive]=useState(false);const ref=useRef(null);function toggle(){if(active){ref.current?.stop();return}const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(!Recognition)return toast.error('Ditado não disponível neste navegador.');const recognition=new Recognition();recognition.lang='pt-BR';recognition.continuous=true;recognition.interimResults=false;recognition.onresult=e=>{let text='';for(let i=e.resultIndex;i<e.results.length;i++)text+=e.results[i][0].transcript;onText(text)};recognition.onerror=()=>{setActive(false);toast.error('O ditado foi interrompido.')};recognition.onend=()=>setActive(false);ref.current=recognition;recognition.start();setActive(true)}return <button type="button" aria-pressed={active} onClick={toggle} className="rounded-full border px-3 py-2 text-xs font-bold">{active?'Parar ditado':'Ditado por voz'}</button>}
